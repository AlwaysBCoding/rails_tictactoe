<% if @response %>
	var response = <%= @response %>;
	var $square = ($("#" + response));
	$square.trigger("click")
<% end %>

var gameOver = <%= @gameOver %>;
var loss = <%= @loss %>;
if(gameOver == true) {
	if(loss == true) {
		$("#you-lose").modal();
	} else {
		$("#draw-game").modal();
	}
}
